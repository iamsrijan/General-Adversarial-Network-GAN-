# General-Adversarial-Network-GAN-
Using generator and discriminator to learn from real image for a noisy image to improve as the generator gets better at forging. The loss for discriminator and generator are tabulated and the improvement of noisy(fake) images is saved.
